# game-data-packager-files

## Includes Games packaged on deb arch and rpm packages for linux

### how to install

Red Hat / Fedora:

```
sudo dnf install ./*.rpm

```

Debian / Ubuntu:

```
sudo apt install ./*.deb
```

Arch Linux:

```
sudo pacman -U *.pkg.tar.zst
```
